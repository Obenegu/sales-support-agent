[build]
builder = "nixpacks"
buildCommand = "dotnet publish backend/SalesSupportBackend/SalesSupportBackend/SalesSupportBackend.csproj -c Release -o out"

[deploy]
startCommand = "dotnet out/SalesSupportBackend.dll"
